import 'package:flutter/material.dart';

class CustomAppsHome extends StatelessWidget {
  const CustomAppsHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("apps"),
      ),
    );
  }
}
